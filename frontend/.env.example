# Copy this file to .env.local and fill in the values

# Database (SQLite — no setup needed for dev)
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="q1zVZyZ6u8X9mQf5bG2nYxJ3o4pT7wV9rKc8lM2hQ0s="   # run: openssl rand -base64 32

# Google OAuth (optional — skip if only using email/password)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Your deployed DRISHTI model (already hardcoded as default, override here if needed)
# DRISHTI_MODEL_URL="https://29deepanshutyagi-drishti-api.hf.space/predict"
